{
  "name": "landing-intel-console",
  "version": "0.2.0",
  "private": true,
  "description": "Landing page intelligence console with Playwright capture and AI analysis.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@clerk/backend": "^2.12.1",
    "playwright-core": "1.60.0"
  }
}
